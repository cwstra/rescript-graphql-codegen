{
  "main": "src/Index.mjs",
  "name": "@re-graphql-codegen/operations",
  "version": "0.0.1",
  "keywords": [
    "rescript"
  ],
  "license": "MIT",
  "author": "cwstra",
  "scripts": {
    "build": "rescript",
    "operations:build": "yarn build",
    "clean": "rescript clean",
    "operations:clean": "yarn clean",
    "fmt": "rescript format",
    "operations:fmt": "yarn fmt",
    "dev": "rescript build -w",
    "operations:dev": "yarn workspaces foreach -R --from @re-graphql-codegen/graphql-codegen run build; yarn dev"
  },
  "dependencies": {
    "@re-graphql-codegen/core-plus": "workspace:*",
    "@re-graphql-codegen/graphql": "workspace:*",
    "@re-graphql-codegen/graphql-codegen": "workspace:*",
    "rescript": "^11.0.1",
    "rescript-embed-lang": "^0.4.0"
  },
  "peerDependencies": {
    "graphql": "^0.12.0 || ^0.13.0 || ^14.0.0 || ^16.0.0"
  }
}
