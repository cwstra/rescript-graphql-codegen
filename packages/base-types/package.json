{
  "main": "src/Index.mjs",
  "name": "@re-graphql-codegen/base-types",
  "version": "0.0.1",
  "keywords": [
    "rescript"
  ],
  "license": "MIT",
  "author": "cwstra",
  "scripts": {
    "build": "rescript",
    "base-types:build": "yarn build",
    "clean": "rescript clean",
    "base-types:clean": "yarn clean",
    "fmt": "rescript format",
    "base-types:fmt": "yarn fmt",
    "dev": "rescript build -w",
    "base-types:dev": "yarn workspaces foreach -R --from @re-graphql-codegen/base-types run build; yarn dev"
  },
  "dependencies": {
    "@re-graphql-codegen/core-plus": "workspace:*",
    "@re-graphql-codegen/graphql": "workspace:*",
    "@re-graphql-codegen/graphql-codegen": "workspace:*",
    "rescript": "^11.0.1"
  },
  "peerDependencies": {
    "graphql": "^0.12.0 || ^0.13.0 || ^14.0.0 || ^16.0.0"
  }
}
