{
  "main": "./dist/Index.js",
  "name": "@rescript-graphql-codegen/base-types",
  "version": "0.0.11",
  "keywords": [
    "rescript"
  ],
  "files": [
    "./dist/Index.js"
  ],
  "license": "MIT",
  "author": "cwstra",
  "scripts": {
    "build": "rescript",
    "base-types:build": "yarn build",
    "clean": "rescript clean",
    "base-types:clean": "yarn clean",
    "fmt": "rescript format",
    "base-types:fmt": "yarn fmt",
    "dev": "rescript build -w",
    "base-types:dev": "yarn lib:build; yarn dev",
    "esbuild": "node esbuild.mjs"
  },
  "peerDependencies": {
    "graphql": "^0.12.0 || ^0.13.0 || ^14.0.0 || ^16.0.0"
  },
  "devDependencies": {
    "@rescript-graphql-codegen/lib": "workspace:*",
    "esbuild": "^0.20.2",
    "rescript": "^11.1.0"
  }
}
