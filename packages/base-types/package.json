{
  "main": "src/Index.mjs",
  "name": "@rescript-graphql-codegen/base-types",
  "version": "0.0.2",
  "bin": "./dist/Index.js",
  "keywords": [
    "rescript"
  ],
  "files": [
    "./dist/Index.js"
  ],
  "license": "MIT",
  "author": "cwstra",
  "scripts": {
    "build": "rescript",
    "base-types:build": "yarn build",
    "clean": "rescript clean",
    "base-types:clean": "yarn clean",
    "fmt": "rescript format",
    "base-types:fmt": "yarn fmt",
    "dev": "rescript build -w",
    "base-types:dev": "yarn workspaces foreach -R --from @rescript-graphql-codegen/base-types run build; yarn dev"
  },
  "dependencies": {
    "@rescript-graphql-codegen/core-plus": "workspace:*",
    "@rescript-graphql-codegen/graphql": "workspace:*",
    "@rescript-graphql-codegen/graphql-codegen": "workspace:*",
    "rescript": "^11.0.1"
  },
  "peerDependencies": {
    "graphql": "^0.12.0 || ^0.13.0 || ^14.0.0 || ^16.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
