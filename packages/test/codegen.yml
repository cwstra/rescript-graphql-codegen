generates:
  src/operations/:
    preset: near-operation-file
    presetConfig:
      extension: .gen.res
      baseTypesPath: graphql/server-types
    schema:
      - src/schema.graphql
    documents:
      - src/operations/*.graphql
